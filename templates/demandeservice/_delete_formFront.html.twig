<form method="post" action="{{ path('app_demandeservice_delete_front', {'id': demandeservice.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demandeservice.id) }}">
    <button style="width:30px;" class="btn btn-block btn-danger btn-sm"><i class="fa fa-trash"></i></button>
</form>
